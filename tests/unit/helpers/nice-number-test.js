import {
  niceNumber
} from '../../../helpers/nice-number';
import { module, test } from 'qunit';

module('NiceNumberHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = niceNumber(42);
  assert.ok(result);
});
